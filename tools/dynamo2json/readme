Tool to read data from DynamoDB, transform it into VeriteCo format and send back to browser.

How to use:
0) Install Node.js version >=0.8.1 - http://nodejs.org/
1) Start server - type "npm start" in current directory
2) Open in browser "localhost:8090" - you will receive html page.
3) Press "Convert" button
